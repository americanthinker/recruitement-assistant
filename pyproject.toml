[project]
name = "recruitment-assistant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aamad>=0.5.0",
    "python-dotenv>=1.2.2",
]
